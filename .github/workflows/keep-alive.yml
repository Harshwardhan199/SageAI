name: Keep Render Services Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping LLM API
        run: curl -fsS https://sageai-llm.onrender.com/

      - name: Ping Backend API
        run: curl -fsS https://sageai-1cge.onrender.com/
